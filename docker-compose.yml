services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile

    container_name: groceries-scraper-dev

    working_dir: /app

    volumes:
      - .:/app

    command: sleep infinity

    tty: true

    env_file:
      - .env